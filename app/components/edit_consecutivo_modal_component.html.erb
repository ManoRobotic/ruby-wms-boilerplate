<!-- Edit Consecutivo Modal -->
<div
  id="edit-consecutivo-modal"
  data-controller="dialog"
  data-dialog-backdrop="edit-consecutivo-modal"
  data-dialog-backdrop-close="true"
  class="pointer-events-none fixed inset-0 z-[9999] grid h-screen w-screen place-items-center bg-transparent opacity-0 backdrop-blur-sm transition-opacity duration-300"
>
  <div
    data-dialog="edit-consecutivo-modal"
    class="relative m-4 p-4 w-11/12 max-w-2xl rounded-lg bg-white shadow-2xl max-h-[95vh] overflow-y-auto border border-gray-300"
  >
    <!-- Modal header -->
    <div class="flex shrink-0 items-center justify-between pb-2 text-base font-medium text-slate-800 border-b border-slate-200">
      Editar Folio
      <button 
        data-dialog-close="true" 
        type="button"
        class="text-gray-400 hover:text-gray-500 transition-colors duration-200">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>
            
    <!-- Modal body -->
    <div id="edit-consecutivo-modal-body">
      <div class="flex justify-center items-center py-8">
        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-emerald-600"></div>
        <span class="ml-2 text-emerald-700">Cargando...</span>
      </div>
    </div>
  </div>
</div>
