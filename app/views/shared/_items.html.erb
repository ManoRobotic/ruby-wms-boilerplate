<% if admin_signed_in? %>
  <%= link_to '#', class: 'text-white hover:underline py-2 rounded-md text-md font-medium' do %>
    <%= icon 'fa-solid', 'shopping-cart' %>
  <% end %>
  <%= link_to 'Edit', '#', class: "mx-2 text-white hover:text-gray-200 font-bold text-xl" %>
  <%#= link_to 'Log out', '#', class: "mx-2 text-white hover:text-gray-200 font-bold text-xl", method: :delete, data: {
    turbo_method: :delete,
    turbo_confirm: 'Are you sure?'
  } %>
<% else %>
  <%= link_to 'Log in', admin_session_path, class: "mx-2 text-white hover:text-gray-200 font-bold text-xl" %>
  <%= link_to 'Sign up', new_admin_registration_path, class: "mx-2 bg-white text-blue-500 px-4 py-2 rounded-full hover:bg-blue-400 hover:text-white transition duration-300 font-bold" %>
<% end %>