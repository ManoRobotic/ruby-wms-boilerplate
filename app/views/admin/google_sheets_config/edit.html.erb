<div class="w-full px-1 mt-12 mb-16 min-h-screen flex flex-col">
  <div class="relative flex flex-col w-full px-6 text-gray-700 bg-white shadow-md lg:px-10 rounded-xl bg-clip-border flex-1">
    <div class="relative p-4 text-gray-700 bg-white bg-clip-border">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-6">
        <div>
          <h1 class="text-2xl font-semibold text-gray-900 mb-2">Editar Configuración Google Sheets</h1>
          <p class="text-gray-600">Configura los datos necesarios para la sincronización con Google Sheets</p>
        </div>
        <div class="flex gap-2 mt-4 md:mt-0">
          <%= link_to "Volver", 
              admin_google_sheets_config_path, 
              class: "px-4 py-2 bg-gray-500 text-white text-sm font-medium rounded-lg hover:bg-gray-600 transition-colors" %>
        </div>
      </div>

      <%= form_with model: @admin, url: admin_google_sheets_config_path, local: true, method: :patch do |form| %>
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <!-- Configuración Básica -->
          <div class="bg-slate-50 rounded-xl p-6 shadow-sm">
            <h3 class="text-lg font-semibold text-slate-900 mb-4">Configuración Básica</h3>
            
            <div class="space-y-4">
              <!-- Google Sheets Enabled -->
              <div>
                <label class="flex items-center">
                  <%= form.check_box :google_sheets_enabled, 
                      class: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
                  <span class="ml-2 text-sm text-gray-700">Habilitar sincronización con Google Sheets</span>
                </label>
                <% if @admin.errors[:google_sheets_enabled].any? %>
                  <p class="mt-1 text-sm text-red-600"><%= @admin.errors[:google_sheets_enabled].first %></p>
                <% end %>
              </div>

              <!-- Sheet ID -->
              <div>
                <label for="sheet_id" class="block text-sm font-medium text-gray-700 mb-2">
                  ID de la Hoja de Google Sheets
                </label>
                <%= form.text_field :sheet_id, 
                    id: "sheet_id",
                    placeholder: "1RK8FZaQZjd-HPcs8ewFxj-YQUZvRB_DO68U25EZ4qDk",
                    class: "block w-full text-sm rounded-lg border-gray-300 focus:border-blue-500 focus:ring-blue-500" %>
                <p class="mt-1 text-xs text-gray-500">
                  Se encuentra en la URL: https://docs.google.com/spreadsheets/d/[ID_AQUI]/edit
                </p>
                <% if @admin.errors[:sheet_id].any? %>
                  <p class="mt-1 text-sm text-red-600"><%= @admin.errors[:sheet_id].first %></p>
                <% end %>
              </div>

              <!-- Auto-detection Info -->
              <div class="p-4 bg-green-50 border border-green-200 rounded-lg">
                <div class="flex items-start">
                  <svg class="h-5 w-5 text-green-600 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                  </svg>
                  <div class="ml-3">
                    <h4 class="text-sm font-medium text-green-800">Auto-detección Inteligente</h4>
                    <p class="text-sm text-green-700 mt-1">
                      ¡Ya no necesitas el GID! El sistema detectará automáticamente la pestaña 
                      que contenga datos de órdenes de producción (columnas como 'no_opro', 'fec_opro', etc.)
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Credenciales de Google -->
          <div class="bg-amber-50 rounded-xl p-6 shadow-sm">
            <h3 class="text-lg font-semibold text-amber-900 mb-4">Credenciales de Google API</h3>
            
            <div class="space-y-4">
              <!-- Current Status -->
              <div class="p-3 rounded-lg <%= @admin.google_credentials.present? ? 'bg-green-100 border border-green-300' : 'bg-red-100 border border-red-300' %>">
                <p class="text-sm <%= @admin.google_credentials.present? ? 'text-green-800' : 'text-red-800' %>">
                  <% if @admin.google_credentials.present? %>
                    ✓ Credenciales configuradas
                  <% else %>
                    ✗ No hay credenciales configuradas
                  <% end %>
                </p>
              </div>

              <!-- Credentials JSON -->
              <div>
                <label for="google_credentials_json" class="block text-sm font-medium text-gray-700 mb-2">
                  JSON de Credenciales de Cuenta de Servicio
                </label>
                <textarea name="admin[google_credentials_json]" 
                          id="google_credentials_json"
                          rows="8"
                          placeholder='{ 
  "type": "service_account",
  "project_id": "tu-proyecto-id",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n",
  "client_email": "tu-cuenta@tu-proyecto.iam.gserviceaccount.com",
  ...
}'
                          class="block w-full text-xs font-mono rounded-lg border-gray-300 focus:border-amber-500 focus:ring-amber-500"></textarea>
                <p class="mt-1 text-xs text-gray-500">
                  Pega aquí el contenido completo del archivo JSON de credenciales de Google.
                  <strong>Deja en blanco si no quieres cambiar las credenciales actuales.</strong>
                </p>
                <% if @admin.errors[:google_credentials].any? %>
                  <p class="mt-1 text-sm text-red-600"><%= @admin.errors[:google_credentials].first %></p>
                <% end %>
              </div>

              <!-- Security Warning -->
              <div class="p-3 bg-yellow-100 border border-yellow-300 rounded-lg">
                <div class="flex items-start">
                  <svg class="h-5 w-5 text-yellow-600 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                  </svg>
                  <div class="ml-3">
                    <h4 class="text-sm font-medium text-yellow-800">Seguridad</h4>
                    <p class="text-sm text-yellow-700 mt-1">
                      Las credenciales se almacenan de forma segura en la base de datos. 
                      Solo tu tendrás acceso a tus propias órdenes de producción.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Botones de Acción -->
        <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-end">
          <%= link_to "Cancelar", 
              admin_google_sheets_config_path, 
              class: "px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition-colors text-center" %>
          <%= form.submit "Guardar Configuración", 
              class: "px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors" %>
        </div>

        <!-- Errores generales -->
        <% if @admin.errors[:base].any? %>
          <div class="mt-4 p-4 bg-red-50 border border-red-300 rounded-lg">
            <% @admin.errors[:base].each do |error| %>
              <p class="text-sm text-red-800"><%= error %></p>
            <% end %>
          </div>
        <% end %>
      <% end %>

      <!-- Instrucciones Detalladas -->
      <div class="mt-8 bg-blue-50 rounded-xl p-6">
        <h3 class="text-lg font-semibold text-blue-900 mb-4">Instrucciones Detalladas</h3>
        
        <div class="space-y-6 text-sm text-blue-800">
          <div>
            <h4 class="font-semibold mb-2">Paso 1: Crear Credenciales en Google Cloud Console</h4>
            <ol class="list-decimal list-inside space-y-1 ml-4">
              <li>Ve a <a href="https://console.cloud.google.com/" target="_blank" class="underline hover:text-blue-600">Google Cloud Console</a></li>
              <li>Crea un nuevo proyecto o selecciona uno existente</li>
              <li>Habilita la API de Google Sheets</li>
              <li>Ve a "Credenciales" → "Crear credenciales" → "Cuenta de servicio"</li>
              <li>Descarga el archivo JSON de credenciales</li>
            </ol>
          </div>
          
          <div>
            <h4 class="font-semibold mb-2">Paso 2: Compartir tu Hoja de Google Sheets</h4>
            <ol class="list-decimal list-inside space-y-1 ml-4">
              <li>Abre tu hoja de Google Sheets</li>
              <li>Haz clic en "Compartir"</li>
              <li>Agrega el email de la cuenta de servicio (aparece en el JSON)</li>
              <li>Dale permisos de "Editor" o "Viewer" según necesites</li>
            </ol>
          </div>
          
          <div>
            <h4 class="font-semibold mb-2">Paso 3: Obtener el ID y organizar datos</h4>
            <ul class="list-disc list-inside space-y-1 ml-4">
              <li><strong>Sheet ID:</strong> Copia desde la URL de tu hoja</li>
              <li><strong>Organiza tus datos:</strong> Asegúrate de que tengas columnas como 'no_opro', 'fec_opro', 'stat_opro', 'clave producto'</li>
              <li><strong>El sistema detectará automáticamente</strong> cuál pestaña contiene los datos de órdenes</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>