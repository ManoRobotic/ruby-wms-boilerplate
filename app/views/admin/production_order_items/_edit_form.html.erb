<%# Use the same form partial as the new consecutivo form, but with an existing item %>
<%= render(ConsecutivoFormComponent.new(production_order: production_order_item.production_order, consecutivo_form: production_order_item)) %>